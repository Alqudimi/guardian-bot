# توثيق نظام الألعاب في Guardian Bot

## 1. النطاق والحالة الحالية

نظام الألعاب جزء داخلي من Guardian Bot، ولا يعتمد في الألعاب المسجلة على Telegram Mini Apps أو مواقع ألعاب خارجية أو بوتات أخرى. الألعاب المتاحة حالياً هي **Mafia** و**Chameleon** فقط، وكل لعبة تطبق منطقها داخل المستودع وترث من `BaseGame`.

يجب ألا تُسجّل أي لعبة جديدة لمجرد وجود زر أو رابط. قبول اللعبة يتطلب منطق لعب فعلي، حالات واضحة، callbacks أو رسائل قابلة للمعالجة، حفظاً واستعادة للحالة، واختبارات تثبت تدفقاً حقيقياً.

## 2. المعمارية العامة

| المكوّن | المسؤولية |
|---|---|
| `BaseGame` | العقد الأساسي الذي يحدد بدء اللعبة، callbacks، الرسائل، الإيقاف، حفظ الحالة، واستعادتها |
| `GameManager` | registry لفئات الألعاب والتحقق من أسماء الألعاب المسجلة |
| `GameSessionManager` | دورة حياة الجلسات، cache محدود في الذاكرة، Redis persistence، TTL، الاستعادة، واكتشاف الجلسة النشطة |
| `src/games/plugins/text_based/` | منطق Mafia وChameleon المملوك للبوت |
| `message_handler.py` | تسجيل الأوامر وتوجيه الرسائل وcallbacks إلى اللعبة النشطة |
| Redis | تخزين حالة الجلسات بين التحديثات وإعادة التشغيل وفق TTL |

يتبع النظام فصلاً عملياً بين منطق الجلسة وتخزينها وبين مسار Telegram. لا تزال واجهة `BaseGame` تستقبل `Update` و`ContextTypes` لأن التكامل الحالي مبني مباشرة على python-telegram-bot، ولذلك يجب عدم وصفها بأنها طبقة مستقلة تماماً عن Telegram.

## 3. عقد BaseGame

تحدد `src/games/base.py` العمليات التالية لكل لعبة:

| العملية | الغرض |
|---|---|
| `start(update, context)` | إنشاء رسالة البداية وإعداد التسجيل أو الجولة |
| `handle_callback(update, context)` | معالجة أزرار Inline Keyboard والتحقق من اللاعب والمرحلة والهدف |
| `handle_message(update, context)` | معالجة الرسائل النصية الخاصة باللعبة |
| `stop()` | إنهاء اللعبة وتنظيف مواردها |
| `get_game_state()` | تحويل الحالة المملوكة للعبة إلى بيانات قابلة للتخزين |
| `load_game_state(state)` | استعادة الحالة بعد تحميلها من Redis |

## 4. Mafia

تبدأ Mafia بمرحلة تسجيل اللاعبين داخل المجموعة. يحدد المضيف بدء الجولة بعد اكتمال العدد، ثم توزع الأدوار وتدخل اللعبة مرحلة الليل. تنفذ Mafia وDoctor وInspector أفعالهم عبر رسائل خاصة وأزرار callbacks، بينما تعالج اللعبة النتائج عند اكتمال الأفعال المطلوبة. بعدها تبدأ مرحلة النهار والتصويت بين اللاعبين الأحياء، ويُعلن اللاعب المستبعد وتُفحص شروط فوز القرية أو Mafia قبل بدء جولة جديدة أو إنهاء اللعبة.

تشمل الحالة المحفوظة المضيف، اللاعبين، الأدوار، الأحياء، Mafia IDs، الطبيب والمحقق، المرحلة، الجولة، الأهداف، الأصوات، ونتيجة النهاية. لا تعتمد اللعبة على `asyncio.sleep` لإيهام المستخدم بوجود دورة لعب؛ الانتقال بين المراحل يحدث استجابة لتفاعلات اللاعبين الفعلية.

## 5. Chameleon

تبدأ Chameleon بتسجيل اللاعبين، ثم اختيار موضوع الجولة. يرسل البوت الموضوع والكلمة سراً إلى اللاعبين الذين يجب أن يعرفوها، مع تمييز اللاعب Chameleon وفق منطق اللعبة. بعد ذلك يقدم اللاعبون clues بالترتيب عبر المجموعة، ثم يصوتون على اللاعب المشتبه به. عند اكتمال التصويت تعلن اللعبة النتيجة وتنهي الجلسة، وتُزال الكلمة السرية من الحالة النهائية حيث يلزم.

تشمل الحالة المحفوظة الموضوع، الكلمة الحالية أثناء الجولة، هوية Chameleon، ترتيب الدور، مؤشر الدور، clues، الأصوات، والنتيجة. وتوجد اختبارات roundtrip للتحقق من حفظ الحالة واستعادتها.

## 6. أوامر وتوجيه التفاعلات

| الأمر | السلوك |
|---|---|
| `/games` | عرض الألعاب الداخلية المسجلة فقط |
| `/play mafia` | بدء Mafia داخل المحادثة الحالية |
| `/play chameleon` | بدء Chameleon داخل المحادثة الحالية |
| `/mafia_start` | اختصار صريح لبدء Mafia |
| `/cham_start` | اختصار صريح لبدء Chameleon |
| `/music <url أو بحث>` | مدخل صريح لمشغل الموسيقى |
| `/play <url أو بحث>` | يمر عبر dispatcher الرئيسي إلى مشغل الموسيقى عندما لا يكون الوسيط اسم لعبة مسجلة |
| `/stopgame` | إيقاف اللعبة النشطة وحذف جلسة Redis |

يملك `message_handler.py` تسجيل `/play` الوحيد. إذا كان أول وسيط اسماً مسجلاً في `GameManager` يبدأ اللعبة، وإلا يفوض الاستعلام إلى `src.features.voice_chat.cmd_play`. أزيل تسجيل `/play` المكرر من `voice_chat`، وأصبح `/music` مدخله الصريح. هذا يمنع اعتماد السلوك على ترتيب handlers.

تستخدم callbacks الخاصة بالألعاب بادئة `game:`. في callbacks القادمة من الرسائل الخاصة قد يُمرر `chat_id` الخاص بجلسة المجموعة في نهاية البيانات حتى تستعيد اللعبة الصحيحة بدلاً من إنشاء جلسة مرتبطة بالمحادثة الخاصة.

## 7. إدارة الجلسات وRedis

يبحث `GameSessionManager` أولاً في cache الذاكرة، ثم في Redis عند عدم وجود الجلسة محلياً. تُحفظ الحالة بصيغة JSON مع TTL، وتُستخدم `scan_iter` لاكتشاف الجلسات النشطة دون أمر Redis حاجب. تدعم القراءة مفاتيح legacy عند الحاجة، وتعالج JSON الفاسد بحذف الجلسة المتضررة بدلاً من تمرير حالة غير موثوقة إلى منطق اللعبة.

يجب على أي لعبة جديدة أن تجعل كل بياناتها المهمة قابلة للتسلسل، وأن تختبر إنشاء الجلسة وحفظها وإخلاء الذاكرة ثم استعادتها من Redis. لا يجوز الاعتماد على cache الذاكرة وحده في لعبة متعددة الخطوات.

## 8. الاختبارات

تغطي الاختبارات الحالية تسجيل الألعاب المملوكة للبوت، تدفق Mafia من التسجيل إلى الليل والتصويت، تدفق Chameleon من الموضوع إلى clues والتصويت، roundtrip لحالة اللعب، وإعادة تحميل Mafia من Redis محلي حقيقي. كما تغطي اختبارات hardening بناء التطبيق وملكية `/play` الوحيدة ووجود `/music` وتوجيه أسماء الألعاب أو استعلامات الموسيقى إلى المسار الصحيح.

أوامر التحقق الأساسية:

```bash
python -m compileall -q -f .
python -m pytest tests/test_all_games.py tests/test_game_session.py -q -W error
python -m pytest tests/ -q -W error
```

لا تثبت اختبارات البيئة المعزولة اتصال Bot API حي أو تسليم رسائل Telegram؛ هذا يتطلب token حقيقياً ومجموعة اختبار مخصصة. لكنها تثبت منطق الحالة وRedis persistence وrouting داخل التطبيق، مع استخدام العزل فقط عند الحاجة لعزل API خارجي غير مناسب للاختبار المحلي.

## 9. قواعد التوسع

عند إضافة لعبة، يجب وضعها في plugin داخلي واضح، وتسجيلها من خلال `src/features/register.py`، وإضافة اختبارات لسلوكها الإيجابي والسلبي وحالات النهاية والاستعادة. يجب رفض payloads غير الصالحة وأسماء الألعاب غير المسجلة، ومنع تشغيل لعبتين في المحادثة نفسها إذا كان ذلك يخالف سياسة الجلسات الحالية.

لا ينبغي استخدام Web App أو رابط خارجي كبديل عن منطق لعبة داخلي. وإذا كان المطلوب ميزة خارج نطاق الألعاب، فيجب تسجيلها كميزة مستقلة باسم وأمر غير متعارضين مع `/play` أو أوامر الألعاب.
